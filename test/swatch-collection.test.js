// --------------------------------------------------
// IMPORT MODULES
// --------------------------------------------------
// Node

// Vendor
import test from 'ava';

// Project
import SwatchCollection from '../dist/lib/modules/swatch-collection';

// --------------------------------------------------
// DECLARE TESTS
// --------------------------------------------------
// MODULE TESTS
test.todo( 'MODULE should be importable.' );

// CLASS TESTS
test.todo( 'CLASS should expose the `extractColors()` method.' );

test.todo( 'CLASS should be instantiable' );

test.todo( 'CLASS should throw an error when invoked with missing or invalid arguments.' );

// INSTANCE TESTS
test.todo( 'INSTANCE should expose the `transparent()` method.' );

test.todo( 'INSTANCE should expose the `opaque()` method.' );
